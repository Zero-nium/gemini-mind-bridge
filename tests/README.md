# Protocol Unit Tests

To run the protocol unit test harness using the macOS standard library Python test runner, execute the following command from the project root:

```bash
python3 -m unittest discover tests
```
